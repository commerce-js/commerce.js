# 2026-02-22 — Saved Card Flow Complete

**Phase:** Cloud Identity (Saved Cards milestone)
**Branch:** `main`
**Commits:** `8ad28b0` → `b015146` + phone constraint fix (6 commits)

## Completed

- `saveCard: true` in `cart-pay.post.ts` — Tap now saves cards on payment
- Server-side `tapCustomerId` persistence in `cart-confirm.get.ts` after 3DS capture
- `customerId` option added to `CheckoutSession.submitPayment()` — fixes Tap error 1107
- Both `cart-pay.post.ts` and `pay.post.ts` pass `tapCustomerId` for saved card charges
- `v-show` on card form containers to preserve Tap SDK state
- Reactive `shouldShowCardForm` computed replaces 3 fragile watchers + onMounted
- Dropped `profiles_phone_key` UNIQUE constraint (DB + Drizzle schema)
- Saved card flow working end-to-end: save → fetch → tokenize → pay

## Files Modified

- `packages/checkout/src/checkout-session.ts` — `customerId` in `submitPayment()`
- `packages/platform/src/database/drizzle/schema/profiles.ts` — phone no longer unique
- `apps/hosted-checkout/server/api/cart-pay.post.ts` — `saveCard: true` + `customerId`
- `apps/hosted-checkout/server/api/cart-confirm.get.ts` — `tapCustomerId` persistence
- `apps/hosted-checkout/server/api/sessions/[id]/pay.post.ts` — pass `customerId`
- `apps/hosted-checkout/app/pages/[id]/index.vue` — SDK init consolidation
- `apps/hosted-checkout/app/pages/pay/cart.vue` — SDK init consolidation

## Next Task

1. Add saved cards support to `/pay/[id]` payment links (no OTP/profile currently)
2. Loading spinner while Tap Card SDK initializes after OTP
3. Delivery provider integration (Armada/Parcel)
