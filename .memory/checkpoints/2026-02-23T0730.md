# 2026-02-23 — Payment Link Saved Cards + SDK Spinner

**Phase:** Cloud Identity (Payment Links milestone)
**Branch:** `main` (uncommitted)

## Completed

- Full profile/OTP/saved-card flow added to `/pay/[id]` payment link page
- Email blur → profile lookup → OTP → saved cards → tokenize → pay
- `tapCustomerId` passed to Tap SDK + server pay endpoint
- `saveProfileAfterPayment()` persists tapCustomerId after payment
- Reactive `shouldShowCardForm` computed + watcher (same pattern as cart.vue)
- SDK loading spinner ("Loading card form...") added to all 3 checkout pages
- Build verified (zero errors), browser tested on localhost

## Files Modified

- `apps/hosted-checkout/app/pages/pay/[id].vue` — full rewrite with profile/OTP/saved cards
- `apps/hosted-checkout/app/pages/[id]/index.vue` — SDK loading spinner added
- `apps/hosted-checkout/app/pages/pay/cart.vue` — SDK loading spinner added (both views)

## Next Task

1. Commit this work
2. Delivery provider integration (Armada/Parcel)
3. Tamara (BNPL) provider research → implementation
