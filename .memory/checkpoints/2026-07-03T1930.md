# Checkpoint — 2026-07-03T1930

## TL;DR

Reconciled a broken handoff, then shipped a real security fix + a green-baseline
fix + the M0.5 docs/CI reorg. Working branch `claude/commercejs-m0-m05-push-il5lep`
is fast-forwarded onto production-live `origin/fly/eaas` (`6c10626`) and carries
7 commits. All §7 gates green. **Nothing pushed to `fly/eaas`; deploy is the
owner's call.**

## Context reconciliation (important)

The session began from a handoff describing an "M0" reconciliation (11 commits
`c1923e7`→`0ed6a6c` on top of `fly/eaas`, plus an agentic-workflow plugin at
`9c90b67`). **None of that exists in this container** — the commits are absent
from the object store, reflog, and remote; `origin/claude/…mus9v7` still points
at the stale pre-pivot `cd3acf7`; the agentic-workflow plugin, `docs/WORKFLOW.md`,
and the binding plan `cached-forging-penguin.md` are not present anywhere. That
work lived only in a prior, now-reclaimed container and was never pushed — it is
**unrecoverable from here**. The two *inputs* survive on the remote
(`fly/eaas` = canonical/live; `mus9v7` = the abandoned parallel build), but
`fly/eaas` long ago moved past `mus9v7` (merchant-admin T01–T05, storefront-eaas,
live on Fly `fra`), so the "merge mus9v7 into fly/eaas" framing is moot.

Decision taken: do **not** force-push `mus9v7` (impossible + against this
session's branch policy). Fast-forward `…il5lep` onto real `fly/eaas` (clean FF —
`87acac0` is an ancestor of `6c10626`), then do the parts of the handoff that are
real and premise-independent.

## What shipped this session (7 commits on `…il5lep`)

1. `56b5276` **fix(dashboard): fail-closed session seal.** The three sealed-cookie
   utils (operator/merchant/buyer) fell back to a hardcoded 32-char key when
   `NUXT_SESSION_PASSWORD` was missing/short — **in production too** (forgeable
   cookies). New `server/utils/sessionSeal.ts` (pure, unit-tested policy) throws
   in prod, dev fallback only under `import.meta.dev`. New Nitro plugin
   `00-validate-session-seal.ts` refuses to boot in prod when the seal is weak.
   `/api/_health` reports `sessionSealSecure`. New offline dashboard vitest
   project + 7 tests. **Boot-verified both ways** (see Verification).
2. `1513b6b` **fix(core): declare `@types/node`.** `@commercejs/core` used
   `setTimeout`/`globalThis.fetch` without declaring `@types/node`; `.npmrc`
   doesn't hoist `@types/*`, so `tsc` failed with 5 errors on `fly/eaas` —
   undetected because CI never ran on the branch. Also cast a `vi.fn()` mock to
   `typeof fetch`. Restores `build typecheck` to 37/37.
3. `b44303f` **chore(repo): root hygiene.** `CI_CD_GUIDE.md` → `CONTRIBUTING.md`;
   delete committed `coverage/` + gitignore `coverage/`/`node-compile-cache/`;
   un-ignore `.memory/`; README marks `@commercejs/cloud`+`cli` frozen (main only).
4. `b770600` **docs(plans): `.plans/README.md` index + archive** superseded plans;
   rename `marketing.doc`→`.md`; fix grand-plan dead `.agent/`/`.research/` links.
5. `98762a6` **docs(claude): rewrite CLAUDE.md** for live `fly/eaas` reality
   (kill dead `.agent/*`; 3-step start; branch map + LIVE DEPLOYMENTS; top-8
   decisions inline). Append 2 real traps to `.memory/gotchas.md`.
6. `2daf09d` **ci(workflows): run on fly/eaas** + dashboard job (prisma generate,
   offline tests, build, worker `--dry-run` from a `pnpm deploy` tree) + turbo
   cache + coverage artifact. Add `worker.ts --dry-run`. New `docker.yml`
   (build, no push) + `fly-deploy.yml` (gated on `FLY_API_TOKEN`, skips otherwise).
7. `18718d2` **fix(ci): pin flyctl-actions to a commit SHA** — the one confirmed
   security-review finding (mutable `@master` in a job holding the deploy token).

## Verification (all green)

- `pnpm install` → 0. `build typecheck` (excl. dashboard/docs) → **37/37**.
  `vitest run` → **301 passed** (294 + 7 new dashboard tests). query-parity →
  "in sync". dashboard build (nuxt + worker bundle) → 0. worker `--dry-run`
  (from `pnpm deploy` tree) → exit 0, config presence printed.
- **Boot smoke**: strong secret → serves, `/api/_health` `sessionSealSecure:true`.
  No secret (prod) → **process exits 1, serves nothing**, error is exactly the
  plugin's "refusing to start" — fail-closed confirmed end-to-end.
- Review gate (independent reviewer + security agents, adversarially verified):
  4 raw → 1 confirmed (the flyctl pin, fixed) → 0 open.

## Open OWNER decisions (surfaced, not decided)

1. **Lost M0 work** — do you have `0ed6a6c` recoverable elsewhere? If not, the
   API-key mint/revoke + role-gated control-plane mutations it added are still
   missing from `fly/eaas` and would need re-deriving against current code.
2. **Neon sharding conflict** — repo's `.memory/decisions.md` (2026-03-12) locks
   **branch-per-merchant**; the handoff guardrail said **project-per-merchant**.
   CLAUDE.md now states the repo-locked branch-per-merchant + flags this. Which is it?
3. **Region** — live on `fra`; `bah` (GCC) is the stated target. Settle it.
4. **agentic-workflow plugin** — the handoff's required execution vehicle is not
   installed here. Reinstall it, or drop the requirement?
5. **Changeset** — none added (no published surface changed). The handoff's
   "@commercejs/platform minor changeset" was tied to the lost work.

## Next

Push `claude/commercejs-m0-m05-push-il5lep` to origin — a clean **fast-forward**
(its remote is still at `87acac0`, an ancestor of these commits; **no force
needed, no `mus9v7`, no touch to `fly/eaas`**). Then: owner reviews, decides
#1–#5, and owns any merge/deploy.
