# Checkpoint — 2026-02-26T08:00

## What happened
1. **Hosted checkout deployed** to Cloudflare Pages (`commercejs-checkout`) with all delivery features (Armada OAuth, map, estimates, dispatch)
2. **Storefront delivery** added to `checkout.vue`: Pickup/Delivery toggle, Google Maps + geolocation, reverse geocoding, coordinates in address metadata
3. **Stale cart bug fixed** — `useCart` composable now auto-recovers when `commerce_cart_id` cookie points to a deleted cart (creates fresh cart + retries)
4. **Delivery job script** committed (`packages/platform/scripts/create-delivery-job.cjs`)

## Key commits
- `0db249e` — Delivery integration with Armada + Google Maps (25 files, 1818 insertions)
- `027490d` — Storefront delivery support in checkout
- `f2d1b66` — Fix stale cart auto-recovery
- `684ca83` — Add delivery job creation script

## State of the system
- **Hosted checkout:** Deployed at `commercejs-checkout.pages.dev` with delivery UI
- **Storefront:** Delivery toggle + map in checkout, committed but Cloudflare needs `GOOGLE_MAPS_KEY` env var
- **Dashboard:** Armada OAuth install flow works via Cloudflare tunnel
- **Armada dispatch:** Blocked on merchant balance top-up (code is ready)
- **Cart composable:** Auto-creates cart on addItem if none exists, auto-recovers from stale IDs

## Files changed this session
- `apps/storefront/app/pages/checkout.vue` — fulfillment toggle, map, geolocation
- `apps/storefront/nuxt.config.ts` — `googleMapsKey` in runtimeConfig
- `apps/storefront/.env` — `GOOGLE_MAPS_KEY`
- `packages/nuxt/src/runtime/composables/useCart.ts` — stale cart recovery
- `packages/platform/scripts/create-delivery-job.cjs` — utility script
