# 2026-02-22 — Profile API Routes + OTP Complete

**Phase:** Cloud Identity (API milestone)
**Branch:** `phase/cloud-identity`
**Commit:** `5d4bd81` — feat(platform,hosted-checkout): add Profile API routes and OTP verification

## Completed

- `profile_otp_codes` table (Drizzle schema + migration + Neon)
- OTP query functions (create, find, verify, increment, cleanup)
- OTP service (`otp.ts`) with 6-digit code, 5-min TTL, rate limiting, dev console logging
- 4 API routes:
  - `POST /api/profile/lookup` — find/create profile by email or phone
  - `POST /api/profile/otp/send` — generate + send OTP
  - `POST /api/profile/otp/verify` — validate code, return full profile
  - `POST /api/profile` — save/update profile + address + payment method
- `createProfileDomain` + all profile queries exported from platform barrel
- Hosted-checkout typecheck passes for all new files

## Next Task

1. Integrate profile into hosted-checkout frontend (cart.vue)
2. Profile lookup + OTP UI at checkout
3. Auto-fill from verified profile
4. Post-purchase "Save to profile" opt-in
