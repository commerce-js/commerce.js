# Delivery Provider Integration — Checkpoint

**Date:** 2026-02-23T09:00+03:00
**Status:** Complete (uncommitted)

## Completed

1. **@commercejs/core** — delivery provider wiring
   - `events.ts`: 4 new delivery events (`estimated`, `created`, `updated`, `cancelled`)
   - `commerce.ts`: `delivery`/`defaultDelivery`/`autoDispatch` config, `getDeliveryProvider()` helper, 5 instance methods
   - `commerce.test.ts`: 8 new tests (59 total, all passing)
   - Built successfully (22.33 KB)

2. **@commercejs/nuxt** — delivery API routes
   - `useServerDeliveryProvider()` utility in `server/utils/delivery.ts`
   - Delivery Zod schemas in `server/utils/schemas.ts`
   - 5 routes under `_commerce/delivery/`: estimate, create, get, cancel, webhook
   - Built successfully (84.7 KB)

3. **hosted-checkout** — delivery endpoints
   - `delivery-estimate.post.ts` — checkout-time cost estimation
   - `delivery-dispatch.post.ts` — admin-triggered dispatch with order status update

## Decision
- **Dispatch model:** separate admin action with optional `autoDispatch` config flag

## Next
- Commit all changes
- Next task from roadmap (Tamara BNPL or storefront readiness)
