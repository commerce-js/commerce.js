# Storefront Search & Polish — Checkpoint

**Date:** 2026-02-24T05:20+03:00
**Status:** Complete (uncommitted)

## Completed

1. **Product Search Modal** — `SearchPalette.vue` component
   - `UModal` + `UCommandPalette` with real-time API search
   - `⌘K` keyboard shortcut support
   - `ClientOnly` wrapper to avoid Radix `DialogRoot` SSR hydration mismatches
   - SSR fallback link to `/products` for server render

2. **Case-Insensitive Search** — platform `search` operator
   - Added `search` op to Drizzle queries: `OR(ILIKE name, ILIKE description)`
   - Added `search` op to Prisma queries: `OR(contains name, contains description)` with `insensitive` mode
   - Changed catalog domain from `like` → `search` operator
   - Rebuilt `@commercejs/platform` package

3. **Currency Display Fix** — `price.formatted` instead of `price.currencyCode`

4. **Tailwind Lint Cleanup** — ~65 verbose CSS variable classes replaced
   - `bg-(--ui-bg)` → `bg-default`, `text-(--ui-text-muted)` → `text-muted`, etc.
   - `aspect-[4/3]` → `aspect-4/3`, `aspect-[3/4]` → `aspect-3/4`
   - `bg-gradient-to-*` → `bg-linear-to-*`

## Files Modified

- `apps/storefront/app/components/SearchPalette.vue` — [NEW] search modal component
- `apps/storefront/app/layouts/default.vue` — integrated SearchPalette, Tailwind cleanup
- `apps/storefront/app/pages/index.vue` — Tailwind cleanup (aspect, gradient)
- `apps/storefront/app/pages/products/index.vue` — Tailwind cleanup (aspect)
- `apps/storefront/app/pages/categories/[slug].vue` — Tailwind cleanup (aspect)
- `apps/storefront/app/error.vue` — Tailwind cleanup
- `packages/platform/src/database/drizzle/queries/catalog.ts` — ILIKE + search operator
- `packages/platform/src/database/prisma/queries/catalog.ts` — search operator for Prisma
- `packages/platform/src/domains/catalog.ts` — uses `search` op for product queries

## Next

- Commit all changes
- T07: Build & E2E storefront validation
- Delivery provider integration follow-up (Armada/Parcel adapters exist, need real API testing)
