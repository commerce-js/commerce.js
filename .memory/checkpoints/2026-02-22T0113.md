# 2026-02-22 — Profile Schema Complete

**Phase:** Cloud Identity (schema milestone)
**Branch:** `phase/cloud-identity`
**Commit:** `8827004` — feat(types,platform): add Profile schema, types, queries, and domain

## Completed

- `Profile`, `SavedAddress`, `SavedPaymentMethod`, `ProfileMerchantLink` types in `@commercejs/types`
- 4 Drizzle tables: `profiles`, `profile_addresses`, `profile_payment_methods`, `profile_merchant_links`
- 14 Drizzle query functions + Prisma parity queries (dormant)
- `createProfileDomain()` factory in `@commercejs/platform`
- Migration applied to Neon — all 4 tables verified
- Both packages build clean (zero errors)

## Next Task

1. Add Profile API routes to `apps/hosted-checkout`:
   - `POST /api/profile/lookup`
   - `POST /api/profile/verify` (email OTP)
   - `POST /api/profile/confirm-otp`
   - `POST /api/profile/save`
2. Integrate Profile into checkout flow (`cart.vue`)
3. Add `profile.recognized` / `profile.verified` / `profile.created` events to `@commercejs/core`
4. End-to-end test: checkout → profile lookup → OTP → auto-fill

## Blockers

- Email OTP: can reuse existing Resend notification provider
- SMS OTP: still needs a new provider (not blocking — email-only for Phase 1)

## References

- [Strategy doc](file:///Users/baker/.gemini/antigravity/brain/826dd0dc-f1cc-47df-b6ed-d5523783c16c/link-experience-strategy.md)
- [Impact analysis](file:///Users/baker/.gemini/antigravity/brain/826dd0dc-f1cc-47df-b6ed-d5523783c16c/impact-analysis.md)
