# Dependency Audit — Checkpoint

**Date:** 2026-02-25T05:40+03:00
**Status:** Complete (committed)

## Completed

1. **Drizzle Stack** — full upgrade + compatibility fix
   - `drizzle-orm` 0.38.4 → 0.45.1
   - `drizzle-kit` 0.30.6 → 0.31.9
   - `@neondatabase/serverless` 0.10.4 → 1.0.2
   - Fixed: drizzle-orm 0.38 was incompatible with neon 1.0 (tagged-template API change)
   - Commit: `4af5b89`

2. **Safe Dependencies** — patch/minor updates
   - `bcrypt-ts` 5→8, `nodemailer` 7→8, `resend` 4→6, `tsdown` 0.12→0.20, `minimark` 0.2→1.0
   - Commit: `2ce925e`, `4af5b89`

3. **@nuxt/module-builder** — 0.8.0 → 1.0.2
   - Added `vue-tsc` (required by unbuild v3)
   - Disabled `declarationMap` (rollup-plugin-dts + TS 5.9 compat)
   - Updated exports `.d.ts` → `.d.mts`
   - Commit: `829e0f2`

4. **vitest** — unified entire monorepo on 4.0.18
   - `platform` and `core` were on 3.x, now all packages on 4.0
   - Commit: `8369d9b`

5. **Test Infrastructure** — platform tests now run against real Neon DB
   - Added `resetDb()` for test isolation
   - vitest.config.ts loads `.env` automatically
   - TRUNCATE CASCADE cleanup between tests
   - Fixed count(*) type coercion (string → number)
   - 60s timeouts for remote DB roundtrips
   - Excluded dormant Prisma test (WASM runtime issue)
   - **46/46 Drizzle tests pass**, **59/59 core tests pass**

## On Hold
- `vue-router` 4 → 5 — waiting for Nuxt adoption
- `h3` 1.x → 2.0 — still RC

## Next
- Tamara BNPL integration
- Delivery provider live API testing
- E2E storefront validation
