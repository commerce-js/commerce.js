# Dependencies
node_modules/

# Build output
dist/
.output/
.nuxt/
.wrangler/
*.db

# Turborepo
.turbo/

# Test coverage (generated by `vitest run --coverage`)
coverage/

# Node's on-disk compile cache (NODE_COMPILE_CACHE) — build artifact
node-compile-cache/

# OS
.DS_Store
Thumbs.db

# IDE
.vscode/
.idea/
*.swp
*.swo

# Environment
.env
.env.local
.env.*.local

# Logs
*.log
npm-debug.log*
pnpm-debug.log*

# Agent skills/config
.agents/
.agent/
.tmp/

# Design assets and reference templates (local only)
.templates/
.research/
# NOTE: .memory/ is intentionally NOT ignored — it's the versioned
# cross-session knowledge base (checkpoints, decisions, gotchas) and is
# committed with the code that produced it.

apps/dashboard/.data

.secrets